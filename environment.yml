name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - xarray==0.19.0
  - pip:
    - fvcore
    - scikit-image
    - timm
    - tqdm
prefix: /home/shehel/miniconda3/envs/simvp
