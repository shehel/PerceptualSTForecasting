name: SimVP
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - numpy
  - hickle
  - pip
  - python
  - pytorch
  - xarray
  - pip:
    - nni
    - scikit-image
    - timm
    - tqdm
prefix: /home/shehel/miniconda3/envs/simvp
